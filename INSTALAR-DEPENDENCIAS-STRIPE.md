# 📦 Instalar Dependências Stripe

Execute este comando no terminal do seu MacBook, dentro da pasta do projeto:

```bash
cd /Users/guilhermeteixeira/Documents/PROJETOS/brutal-team

npm install stripe @stripe/stripe-js @stripe/react-stripe-js nanoid
```

## O que será instalado:

- **stripe** - SDK do Stripe para Node.js (backend)
- **@stripe/stripe-js** - SDK do Stripe para JavaScript (frontend)
- **@stripe/react-stripe-js** - Componentes React do Stripe (pagamentos embedded)
- **nanoid** - Gerador de IDs únicos (para tokens de convite)

## Após instalar:

1. Volte aqui e me confirme que instalou
2. Vou continuar criando os arquivos necessários
